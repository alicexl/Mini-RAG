# Mini-RAG 环境变量（可选覆盖）
#
# demo.py 通过 os.environ.get() 读取下面两个变量，都带默认值。
# 本地默认开箱即用：连 http://localhost:11434 + qwen3-embedding:0.6b（1024 维）。
# 只在需要改 Ollama 地址或换 embedding 模型时才设。
#
# 注意：本项目不自动加载 .env（没用 python-dotenv）。
# 需要覆盖时，运行前在 shell 里 export，或写进系统/用户环境变量：
#   export OLLAMA_BASE_URL=http://localhost:11434
#   export OLLAMA_EMBED_MODEL=qwen3-embedding:0.6b

# Ollama 服务地址
# OLLAMA_BASE_URL=http://localhost:11434

# Embedding 模型
# OLLAMA_EMBED_MODEL=qwen3-embedding:0.6b
